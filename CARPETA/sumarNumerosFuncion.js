/*function sumarNumeros(arr) {
  // Inicializamos una variable llamada 'suma' para almacenar la suma de los números.
  let suma = 0;

  // Usamos un bucle 'for' para sumar los números en el arreglo.
  for (let i = 0; i < arr.length; i++) {
    suma += arr[i];
  }

  // Usamos un bucle 'while' para sumar los números en el arreglo.
  let j = 0;
  while (j < arr.length) {
    suma += arr[j];
    j++;
  }

  // Usamos un bucle 'for...of' para sumar los números en el arreglo.
  for (const num of arr) {
    suma += num;
  }

  // Devolvemos la suma total.
  return suma;
}

// Ejemplo de uso:
const numeros = [1, 2, 3, 4, 5];
const resultado = sumarNumeros(numeros);
console.log(`La suma de los números en el arreglo es: ${resultado}`);*/
